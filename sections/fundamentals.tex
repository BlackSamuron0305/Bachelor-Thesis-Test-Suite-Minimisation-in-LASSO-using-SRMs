\chapter{Fundamentals}\label{chap:fundamentals}

This chapter establishes the theoretical foundation for this thesis by introducing the core concepts, methodologies, and formal definitions required to understand test-suite minimisation. We present the metrics employed to quantify test-suite effectiveness, provide a detailed description of the \emph{Stimulus--Response Matrix} (SRM) representation utilised within the LASSO platform, and formally define the test-suite minimisation problem within the context of computational complexity theory.

\section{Test Coverage and Mutation Score}

\emph{Test coverage} quantifies the proportion of program elements (statements, branches, paths, or conditions) exercised by a test suite~\cite{zhu1997}. Coverage metrics indicate structural thoroughness but provide limited insight into fault-detection capability.

\subsection{Coverage Metrics}
\emph{Test coverage} measures which parts of a program are executed during test execution. Different coverage criteria define what constitutes a ``part'' of the program, each providing different levels of granularity and testing thoroughness~\cite{zhu1997}. The following coverage types are commonly employed in test-suite evaluation:

\begin{itemize}
  \item \textbf{Instruction Coverage:} measures the proportion of bytecode instructions executed by the test suite. This is the finest-grained coverage metric, capturing low-level execution at the virtual machine level.
  
  \item \textbf{Line Coverage:} measures the proportion of source code lines executed. A line is considered covered if at least one instruction from that line is executed. This metric provides intuitive feedback directly tied to source code.
  
  \item \textbf{Branch Coverage:} measures the proportion of conditional branches (if-statements, loops, switch cases) whose true and false outcomes are both executed. Branch coverage ensures that both paths from each decision point are tested.
  
  \item \textbf{Method Coverage:} measures the proportion of methods invoked during test execution. A method is considered covered if it is called at least once, regardless of internal execution paths.
  
  \item \textbf{Complexity Coverage:} based on cyclomatic complexity, this metric measures the proportion of independent execution paths exercised. Higher complexity indicates more decision points and potential execution paths.
  
  \item \textbf{Class Coverage:} measures the proportion of classes instantiated or whose static methods are invoked during testing. This coarse-grained metric indicates overall system utilisation.
\end{itemize}

Comprehensive test evaluation requires multi-level coverage metrics that capture program execution at different granularities. Different coverage types detect different classes of faults~\cite{zhu1997}---a test suite that achieves high line coverage but low branch coverage may miss faults in conditional logic, while high method coverage without adequate complexity coverage may fail to exercise all execution paths within complex methods. Employing coverage metrics across the entire spectrum from fine-grained instruction-level execution to coarse-grained class-level utilisation provides a more complete assessment of test-suite quality and fault-detection capability.

\subsection{Mutation Score}

\emph{Mutation testing} is a fault-based technique that quantifies the effectiveness of a test suite by measuring its ability to detect artificially introduced faults, known as \emph{mutants}~\cite{jia2011}. Each mutant is a slightly modified version of the original program, created by applying a syntactic change such as replacing arithmetic or logical operators, altering conditional expressions, or modifying constants. A test suite is then executed against both the original and the mutated versions to determine whether it can distinguish them.

The \emph{mutation score} expresses the proportion of mutants detected by the test suite and is defined as:
\[
\text{Mutation Score} = \frac{\text{Number of mutants killed}}{\text{Total number of mutants}} \times 100\,\%.
\]
A mutant is considered \emph{killed} when a test produces a different observable outcome for the mutant than for the original program. This typically means that the test passes on the original system but fails when executed on the mutant. Conversely, if a test yields the same outcome on both versions, the mutant \emph{survives}.

\paragraph{Mutation Types.}
Two main types of mutation testing are commonly distinguished~\cite{offutt2000}:
\begin{enumerate}
  \item \textbf{Weak Mutation:} a mutant is detected if the program’s internal state immediately after executing the mutated statement differs from that of the original program. Detection is based on local state divergence rather than final output.
  \item \textbf{Strong Mutation:} a mutant is detected only if this internal divergence propagates to the program’s externally observable behaviour, such as output values or test verdicts. This form requires execution to completion and comparison of final results.
\end{enumerate}

Weak mutation focuses on local sensitivity to code changes, while strong mutation measures externally visible behavioural differences. Both flavours aim to approximate a test suite’s fault-detection capability, with the mutation score serving as a quantitative indicator of test effectiveness.


\section{Stimulus--Response Matrices}\label{sec:srm}

Stimulus--Response Matrices (SRMs) constitute the central data structure of LASSO's experimental framework for \emph{Test-Driven Software Experimentation} (TDSE)~\cite{Kessel2022}. They provide a unified, language-independent format for recording and comparing the run-time behaviour of software systems under controlled experimental conditions. To understand how SRMs are constructed, it is necessary to first introduce their two input artefacts: \emph{Sequence Sheets} and \emph{Actuation Sheets}.

\subsection{Sequence Sheets and Actuation Sheets}

\textbf{Sequence Sheets} describe the ordered stimuli applied to a system under test. They provide a tabular, human-readable, and directly executable representation of test cases that bridges the gap between code-based unit tests and tabular acceptance tests such as FIT. Each sequence sheet defines a linear sequence of method invocations together with their input parameters and expected outputs. The notation is deliberately language-independent and focuses on behavioural semantics rather than programming syntax.

A typical sequence sheet consists of the following columns:
\begin{itemize}
  \item \textbf{Output column:} the expected result or externally observable state after each operation.
  \item \textbf{Operation column:} the operation or method name to be invoked.
  \item \textbf{Service column:} either the class to be instantiated (\texttt{create}) or the instance on which the operation is called.
  \item \textbf{Input columns:} zero or more arguments or references to previously created objects.
\end{itemize}

Each row represents one stimulus---a method invocation with given inputs and expected output---applied to the system under test. Objects can be instantiated using \texttt{create}, and cell references (e.g., \texttt{A1}, \texttt{D3}) are used to reuse or pass object instances between rows. Parameterised sequence sheets generalise this approach by introducing placeholders (e.g., \texttt{?p1}, \texttt{?p2}) for parameters or implementation classes, which can be substituted at runtime to produce multiple test variants. Thus, sequence sheets define the full set of stimuli for an experiment.

\textbf{Actuation Sheets} capture the observed responses obtained when executing a sequence sheet. They preserve the same structure but replace expected outputs with actual runtime data, thereby forming an \emph{actuation record}. Each actuation record represents one execution of a (stimulus, system) pair and contains both the concrete input values and the resulting outputs. While sequence sheets describe the \emph{intended behaviour}, actuation sheets record the \emph{empirical behaviour} of the implementation. Executing one sequence sheet across multiple systems or configurations therefore produces several actuation sheets.

\begin{figure}[H]
  \centering
  \begin{minipage}[t]{0.45\textwidth}
    \centering
    \textbf{Stimulus (Sequence Sheet)}\\[3pt]
    \begin{tabular}{c|ccc}
      \textbf{Out} & \textbf{Operation} & \textbf{Service} & \textbf{Input} \\
      \hline
      create & \texttt{create} & \texttt{'Stack} & \\
      -- & \texttt{push} & A1 & 42 \\
      42 & \texttt{pop} & A1 & \\
    \end{tabular}
  \end{minipage}
  \hfill
  \begin{minipage}[t]{0.45\textwidth}
    \centering
    \textbf{Observed Behaviour (Actuation Sheet)}\\[3pt]
    \begin{tabular}{c|ccc}
      \textbf{Out} & \textbf{Operation} & \textbf{Service} & \textbf{Input} \\
      \hline
      \texttt{<instance>} & \texttt{create} & \texttt{'Stack} & \\
      \texttt{true} & \texttt{push} & A1 & 42 \\
      \texttt{42} & \texttt{pop} & A1 & \\
    \end{tabular}
  \end{minipage}
  \caption{Relationship between a sequence sheet (stimulus) and its corresponding actuation sheet (observed behaviour).}
  \label{fig:seq-act-sheet}
\end{figure}

\subsection{From Sequence Sheets to Stimulus--Response Matrices}

LASSO generalises the one-to-one mapping of stimuli to actuation results into a multidimensional artefact known as the \emph{Stimulus--Response Matrix} (SRM). An SRM aggregates all actuation results of a given test set executed across multiple systems or system variants, yielding a unified experimental dataset.

Formally, an SRM is defined as
\[
M = [M_{ij}] \quad \text{with} \quad M_{ij} = \text{Actuation}(t_i, v_j),
\]
where
\begin{itemize}
  \item \(t_i \in T\): stimulus derived from a sequence sheet (row),
  \item \(v_j \in V\): system implementation, variant, or version (column),
  \item \(M_{ij}\): actuation record containing the inputs, outputs, and optional analysis attributes.
\end{itemize}

Each actuation record represents the complete observation of executing stimulus \(t_i\) on variant \(v_j\). Core information includes:
\begin{itemize}
  \item input parameters for the invoked operations,
  \item the corresponding outputs or return values,
  \item exceptions or failure states encountered during execution,
  \item optional execution traces or method-level invocations.
\end{itemize}

In addition to these primary fields, SRMs support \emph{analysis attributes}---arbitrary key--value pairs attached at the actuation level to store additional measurements. These may include:
\begin{itemize}
  \item non-functional metrics (e.g., execution time, memory usage),
  \item coverage data such as line, branch, or method coverage,
  \item mutation-testing results and derived mutation scores,
  \item static properties or derived complexity measures.
\end{itemize}

This flexible schema allows SRMs to store both functional and non-functional aspects of software behaviour. Two complementary representations exist:
\begin{enumerate}
  \item \textbf{Black-box view:} captures only aggregated input/output data for each test stimulus.
  \item \textbf{White-box view:} exposes the full internal invocation structure, enabling detailed trace analysis.
\end{enumerate}

\begin{figure}[H]
  \centering
  \begin{tabular}{c|cccc}
    & \textbf{Original} & \textbf{Mutant 1} & \textbf{Mutant 2} & \textbf{Mutant 3} \\
    \hline
    \textbf{Test 1} & pass & fail & pass & fail \\
    \textbf{Test 2} & pass & pass & pass & pass \\
    \textbf{Test 3} & pass & fail & fail & pass \\
  \end{tabular}
  \caption{Example SRM fragment showing per-test outcomes across variants. Behavioural discrepancies indicate killed mutants.}
  \label{fig:srm-mutation-example}
\end{figure}

\subsection{Coverage and Mutation Integration in SRMs}

LASSO integrates structural coverage and mutation data directly into SRMs. Structural coverage is measured using the JaCoCo tool, which records coverage statistics for each test execution ~\cite{jacoco-counters}. JaCoCo reports entity types (instruction, line, branch, method, and class) and associated metrics (e.g., \texttt{COVEREDCOUNT}, \texttt{TOTALCOUNT}, \texttt{COVEREDRATIO}). These are stored in SRM entries under a dedicated \texttt{sheetId} (e.g., \texttt{JACOCO}) as analysis attributes, enabling fine-grained retrieval via SRMPATH queries.  

Mutation analysis is represented similarly. Each mutant is treated as a separate implementation variant \(v_j\). For every stimulus \(t_i\), the SRM cell \(M_{ij}\) contains the observed actuation of that mutant. A mutant is considered \emph{killed} if the response differs from the original implementation:
\[
v_j \text{ is killed by } t_i \iff \text{Response}(t_i, v_j) \neq \text{Response}(t_i, v_\text{orig}).
\]
Comparing these responses across columns enables data-driven computation of mutation coverage directly from the SRM:
\[
\text{Mutation Score} = 
\frac{\text{Number of mutants killed}}{\text{Total number of mutants}} \times 100\,\%.
\]

\subsection{Interpretation and Applications}

SRMs unify all relevant execution data—functional outcomes, non-functional metrics, coverage, and mutation results—into a single language-independent structure. This facilitates a wide range of analyses such as regression testing, differential testing, N-version testing, and mutation-based assessment. In the context of LASSO, SRMs form the empirical foundation for subsequent optimisation tasks such as test-suite minimisation and coverage preservation, discussed in later chapters.








\section{The Test-Suite Minimisation Problem}

The general optimisation goal of test-suite minimisation was introduced in Section~\ref{sec:problem-statement}. This section provides the formal definition, summarises its computational complexity, and contextualises the problem within the LASSO framework before outlining the principal algorithmic categories that will be examined in detail in Chapter~\ref{chap:literature-review}.

\subsection{Formal Problem Definition}

Let $T = \{t_1, t_2, \ldots, t_n\}$ denote a finite test suite (stimuli in LASSO terminology), and let $R = \{r_1, r_2, \ldots, r_m\}$ represent the set of coverage requirements. Each test $t_i \in T$ covers a subset of requirements $C(t_i) \subseteq R$. The coverage achieved by the full suite is:
\[
C(T) = \bigcup_{i=1}^{n} C(t_i).
\]
The \emph{test-suite minimisation problem} seeks the smallest subset $S^* \subseteq T$ that preserves the original coverage:
\[
S^* = \arg \min_{S \subseteq T} |S| \quad \text{subject to} \quad C(S) = C(T).
\]
This is equivalent to the classical \emph{minimum set-cover problem}.

\subsection{Computational Complexity}

Test-suite minimisation is NP-complete~\cite{yoo2012}, as no polynomial-time algorithm can guarantee optimal solutions for arbitrary instances. Practical implementations therefore rely on approximation algorithms~\cite{chvatal1979} and heuristics~\cite{harrold1993} that trade off exactness for scalability. The classical greedy algorithm achieves a logarithmic approximation ratio of $O(\ln m)$, where $m$ is the number of requirements.

\subsection{Test-Suite Minimisation in the LASSO Context}

Within the LASSO framework, the problem is formulated over the data contained in the \emph{Stimulus--Response Matrix} (SRM). Each stimulus $t_i$ corresponds to one row in the SRM and each response object $M_{ij}$ (for system variant $v_j$) includes coverage and mutation attributes collected during execution. For a given system under test (column $v_j$), the requirement set $C(t_i)$ of each stimulus is derived from JaCoCo coverage metrics (e.g., \texttt{LINE\_COVEREDCOUNT}, \texttt{BRANCH\_COVEREDCOUNT}) and mutation-testing results stored in SRM attributes.

The minimisation objective in LASSO is therefore to select a subset of stimuli whose collective coverage and mutation information preserve the full coverage profile recorded in the SRM:
\[
C(S^*)_{\text{SRM}} = C(T)_{\text{SRM}}.
\]
The minimised suite can then be represented as a reduced SRM that retains the same schema as the original matrix but includes only the selected stimuli (rows).

\subsection{Algorithmic Categories}

Approaches to solving the test-suite minimisation problem fall into several main categories, each offering distinct trade-offs between solution quality and computational efficiency:

\begin{itemize}
  \item \textbf{Greedy heuristics:} Iteratively select the test covering the largest number of uncovered requirements. The Harrold--Gupta--Soffa (HGS) heuristic~\cite{harrold1993} extends this idea by prioritising rare requirements.
  
  \item \textbf{Exact optimisation:} Formulate the problem as an integer linear program (ILP) or constraint-satisfaction model. These yield optimal results but scale poorly for large $T$ and $R$~\cite{mongiovi2020}.
  
  \item \textbf{Search-based methods:} Use genetic or evolutionary algorithms that balance suite size and coverage preservation through multi-objective fitness functions~\cite{hussein2020}.
  
  \item \textbf{Data-driven methods:} Exploit structural similarity or clustering in coverage data to identify representative tests~\cite{bach2017}.
\end{itemize}

These algorithmic classes provide the theoretical and practical foundation for the detailed comparison and evaluation presented in Chapter~\ref{chap:literature-review}.
