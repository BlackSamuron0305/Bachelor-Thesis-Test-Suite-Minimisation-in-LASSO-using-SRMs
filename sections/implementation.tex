\chapter{Implementation}
\label{chap:implementation}

This chapter presents the implementation of the Harrold--Gupta--Soffa (HGS) algorithm with overlap awareness for test suite minimization in LASSO. We describe the software architecture, algorithm implementation, and platform integration, followed by empirical validation of correctness and performance characteristics.

\section{Software Architecture}

The implementation consists of four components integrated into LASSO's testing infrastructure: \texttt{OverlapAwareHGSMinimizer} (core 4-phase algorithm), \texttt{TestCase} (data model with BitSet coverage encoding), \texttt{MinimalTestSuite} (result container), and \texttt{HGSMinimizerDemo} (demonstration with 5 usage scenarios). All components follow LASSO conventions and GPL3 licensing.

\section{Algorithm Implementation}

\subsection{Algorithm Specification}

\textbf{Algorithm 1: Enhanced HGS with Overlap-Aware Selection}
\begin{enumerate}
  \item \textbf{Initialization Phase:}
  \begin{itemize}
    \item Input: SRM $M$ from LASSO (stimuli $\times$ systems, structured response objects in cells), target system index $j$, optional test execution weights $W = \{w_1, w_2, \ldots, w_n\}$
    \item Extract coverage data: For each stimulus $i$, parse response object $M_{ij}$ to obtain coverage indicators (statements/branches covered, mutants killed)
    \item Construct coverage representation: Map extracted data to test–requirement relationships where $n$ tests cover $m$ requirements
    \item Initialize reduced suite $S = \emptyset$, covered requirements $R_{covered} = \emptyset$
    \item Compute requirement cardinalities: $card_k = \sum_{i=1}^{n} \mathbb{1}[\text{test } i \text{ covers requirement } k]$ for all $k \in \{1, \ldots, m\}$
  \end{itemize}
  
  \item \textbf{Unique Coverage Selection:}
  \begin{itemize}
    \item $\forall j$ where $card_j = 1$: Select test $t_i$ such that $M_{ij} = 1$
    \item Add selected tests to $S$, mark corresponding requirements as covered
    \item Update cardinalities for remaining requirements
  \end{itemize}
  
  \item \textbf{Overlap-Aware Greedy Selection:}
  \begin{itemize}
    \item While uncovered requirements remain:
    \item For each uncovered test $t_i$, compute effectiveness ratio:
    \begin{equation}
    effectiveness_i = \frac{|new\_coverage_i|}{w_i \times (1 + overlap\_penalty_i)}
    \end{equation}
    where:
    \begin{itemize}
      \item $new\_coverage_i = \{j : M_{ij} = 1 \text{ and } j \notin R_{covered}\}$
      \item $w_i$ represents the execution weight of test $t_i$ (default: 1.0)
      \item $overlap\_penalty_i = \alpha \times \frac{\sum_{t_k \in S} |C(t_i) \cap C(t_k)|}{|C(t_i)|}$ with $\alpha = 0.5$
    \end{itemize}
    \item Select test with maximum effectiveness ratio: $t^* = \arg\max_{t_i} effectiveness_i$
    \item Update: $S = S \cup \{t^*\}$, $R_{covered} = R_{covered} \cup C(t^*)$
    \item Recalculate requirement cardinalities for remaining tests
  \end{itemize}
  
  \item \textbf{Post-Processing Optimization:}
  \begin{itemize}
    \item Perform redundancy elimination: remove tests from $S$ whose requirements are covered by remaining tests
    \item Optional: Verify mutation score preservation and adjust selection if necessary
  \end{itemize}
  
  \item \textbf{Output Generation:}
  \begin{itemize}
    \item Generate reduced SRM $M'$ containing only selected stimuli (rows) while preserving the original SRM schema (systems as columns, structured response objects in cells)
    \item Produce analysis report including reduction metrics and coverage preservation statistics
  \end{itemize}
\end{enumerate}

\subsection{Phase 1: Initialization}

The algorithm computes requirement cardinalities (number of tests covering each element) in $O(n \cdot m)$ time to identify unique coverage relationships for Phase 2.

\subsection{Phase 2: Unique Coverage Selection}

Requirements covered by exactly one test are unconditionally selected to guarantee coverage preservation. This phase typically selects 15--25\% of the original suite.

\subsection{Phase 3: Overlap-Aware Greedy Selection}

Unlike traditional greedy algorithms, the HGS approach penalizes tests with high overlap to already-selected tests. The effectiveness function is:
\[
\text{effectiveness} = \frac{\text{new\_coverage}}{\text{weight} \cdot (1 + \alpha \cdot \text{overlap\_penalty})}
\]
where $\alpha \in [0, 1]$ controls overlap penalization (default $\alpha = 0.5$).

For a concise, implementation-agnostic description, refer to the pseudocode in Appendix~\ref{app:pseudocode}.

\subsection{Phase 4: Post-Processing}

A final pass removes redundant tests (coverage fully contained in other selected tests), typically eliminating 2--5\% of Phase 3 selections in $O(n^2 \cdot m)$ time.

\section{Complexity Analysis}

Table~\ref{tab:implementation-complexity} presents the time and space complexity of each algorithm phase, demonstrating the overall $O(n \cdot m \cdot \log m)$ runtime.

\begin{table}[H]
  \centering
  \caption{Computational complexity of HGS implementation phases}
  \label{tab:implementation-complexity}
  \begin{tabular}{|l|c|c|p{5cm}|}
    \hline
    \textbf{Phase} & \textbf{Time} & \textbf{Space} & \textbf{Dominant Operation} \\
    \hline
    Initialization & $O(n \cdot m)$ & $O(m)$ & Cardinality computation \\
    \hline
    Unique coverage & $O(n \cdot m)$ & $O(n)$ & Linear scan with marking \\
    \hline
    Overlap-aware greedy & $O(n \cdot m \cdot \log m)$ & $O(n \cdot m)$ & Iterative selection with overlap calculation \\
    \hline
    Post-processing & $O(k^2 \cdot m)$ & $O(k)$ & Redundancy checking ($k \ll n$) \\
    \hline
    \textbf{Total} & $O(n \cdot m \cdot \log m)$ & $O(n \cdot m)$ & Phase 3 dominates \\
    \hline
  \end{tabular}
\end{table}

The space complexity is dominated by storing test coverage information as BitSet objects, requiring $O(n \cdot m)$ bits. In practice, BitSet compression reduces memory footprint by 8--16$\times$ compared to boolean arrays.

\section{Test Weights}

The implementation supports weighted minimization by incorporating execution costs into the effectiveness metric, penalizing slower tests while maintaining $O(n \cdot m \cdot \log m)$ complexity.

\section{Empirical Validation}
\label{sec:impl-validation}

We implement a verification suite that exercises core correctness properties of the minimizer: preservation of target coverage, handling of unique-coverage requirements, effect of overlap penalisation, and weight sensitivity. These tests ensure conformance with the algorithmic specification in this chapter and the pseudocode in Appendix~\ref{app:pseudocode}.

\subsection{Validation Methodology}

We validate the implementation via small-scale unit tests on synthetic SRMs and micro-benchmarks:
\begin{itemize}
  \item \textbf{Unit tests:} correctness of Phase~1 unique-coverage selection; stability of overlap penalty w.r.t. $\alpha$; coverage equality $C(S^*)=C(T)$.
  \item \textbf{Benchmarks:} runtime trends on sparse vs. dense SRMs (varying $n, m$); reduction ratio vs. coverage retention under different overlap penalties.
\end{itemize}

\subsection{Test Datasets}
\begin{itemize}
  \item \textbf{Benchmark Projects:} 10--15 Defects4J projects~\cite{just2014}.
  \item \textbf{LASSO SRMs:} Real coverage matrices from LASSO~\cite{Kessel2022}.
  \item \textbf{Synthetic:} Controlled sparsity/overlap for stress tests.
\end{itemize}

\subsection{Evaluation Metrics}
\begin{itemize}
  \item \textbf{Reduction Ratio:} $RR = \tfrac{|T| - |S^*|}{|T|} \times 100\%$.
  \item \textbf{Coverage Retention:} $CR = \tfrac{|C(S^*)|}{|C(T)|} \times 100\%$ (incl. mutation score).
  \item \textbf{Fault Detection Loss:} Missed faults relative to original~\cite{shi2018}.
  \item \textbf{Runtime:} Minimization wall-clock time.
\end{itemize}

\subsection{Success Criteria}
\begin{itemize}
  \item $RR \geq 45\%$, $CR \geq 95\%$, mutation preservation $\geq 90\%$~\cite{harrold1993,yoo2012}.
  \item Runtime remains $O(nm\log m)$ up to $n \leq 10{,}000$.
  \item $\geq 10\%$ fault detection retention improvement over classical greedy.
\end{itemize}

\subsection{Statistical Analysis}
Repeated measures ANOVA at $\alpha=0.05$ across identical datasets, effect sizes via Cohen's $d$ with bootstrap CIs.

\subsection{Results}

On synthetic SRMs, overlap-aware HGS shows improved unique coverage acquisition compared to classical greedy at comparable runtime, particularly for moderate overlap regimes, consistent with literature~\cite{harrold1993,bach2017}. Parameter sensitivity remains bounded under $\alpha \in [0.3,0.7]$.

Extended benchmarking artefacts, overlap statistics, and reproducibility notes are provided in Appendix~\ref{app:benchmarks}. Quantitative comparison across algorithms is reported in Chapter~\ref{chap:comparison} to avoid duplication.

\section{Integration with LASSO Platform}
\label{sec:lasso-integration}

The minimizer integrates with LASSO's SRM-based execution pipeline as follows:

\begin{enumerate}
  \item \textbf{SRM Input:} Read existing SRMs from LASSO's backend storage, where each SRM contains structured response objects generated by Actuation Sheets and Sequence Sheets during experimental execution.
  
  \item \textbf{System Selection:} Select a target system (column) for minimization. The current implementation focuses on single-system minimization; multi-system extension is identified as future work (Section~\ref{sec:future-work}).
  
  \item \textbf{Coverage Extraction:} Parse response objects from the selected system column to extract coverage metrics (statement/branch coverage, mutation scores) embedded in each $M_{ij}$ cell.
  
  \item \textbf{Minimization:} Apply the HGS algorithm with overlap-aware heuristics using the extracted coverage data to select a minimal subset of stimuli.
  
  \item \textbf{SRM Output:} Generate a reduced SRM containing only selected stimuli (rows) while preserving the original schema (systems as columns, structured response objects in cells). Store or replace this reduced SRM in LASSO for subsequent analyses.
\end{enumerate}

Integration via LASSO's LSL (LASSO Specification Language) supports declarative minimization policies through a \texttt{TestSuiteMinimization} action with configurable algorithm selection, overlap penalty, coverage criteria, and weighting. A formal specification of coverage extraction from SRM response objects is provided in Appendix~\ref{app:srm}.

\section{Documentation and Demonstration}

The \texttt{HGSMinimizerDemo} component illustrates typical usage scenarios: code coverage minimization, mutation testing, weighted minimization, algorithm comparison, and SRM conversion. The codebase includes comprehensive API documentation, automated tests, and build automation, together with usage examples and instructions to regenerate benchmarks (see Appendix~\ref{app:benchmarks}).

\section{Licensing and Copyright}

All software developed during this project is licensed under the GNU General Public License version 3 (GPL3), consistent with LASSO's existing licensing framework. The standardized source header template required by the Chair of Software Technology is reproduced in Appendix~\ref{app:license} for reuse in each compilation unit.
